# Prisma-crash
    this course to understand how to work in prisma in sql database.

# Tools
    Node js and TypeScript

# Installation
    **clone the project**
        ```bash
            git clone <repo_url>
        ```
    **install package**
        ``` bash
            npm install
        ```
    